- var title = "", icon=""
-  if (page.category) {
-        title = page.category;
-        icon = 'category';
-    };
-    if (page.tag) {
-        title = page.tag;
-        icon = 'tag';
-    };
-    if (page.archive) {
-        icon = 'archive';
-        if (page.year) title = page.year + (page.month ? '/' + page.month : '');
-        else title = __('archive_a');
-    };
.archive-title.clearfix
    h2(class="#{icon}-icon")=title
    if page.archive
        .archiveslist.archive-float.clearfix
            !=list_archives({format:__('archives_time_format')})
#main.archive-part.clearfix
    #archive-page
        - index=true, desc=false
        - page.posts.each(function(item){
            include article.jade
        - })
        if page.total > 1
            nav#page-nav.clearfix.archive-nav.
                paginator({
                    prev_text: "&laquo; Prev",
                    next_text: "Next &raquo"
                })


