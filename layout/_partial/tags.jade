- page.tag = ''
.archive-title
    h2.tag-icon #{__('tags')}
    - site.tags.sort('name').each(function(item){
        a(data-src="#{config.root}#{item.path}" href="#{item.name}" title="#{item.name}") #{item.name}
            sup #{item.posts.length}
    -})
#main.archive-part
    .all-list-box



